"use strict";

// function isNumberInRange(start, end, number) {
//   return number >= start && number <= end;
// }

// console.log(isNumberInRange(10, 5, 4));

// function normalizeInput(input, to) {
//   return to === "upper" ? input.toUpperCase() : input.toLowerCase();
// }

// console.log(normalizeInput("Big SALE", "lower"));

// function checkForName(fullName, firstName) {
//   const loverFullName = fullName.toLowerCase();
//   const loverFirstName = firstName.toLowerCase();
//   return loverFullName.includes(loverFirstName);
// }

// console.log(checkForName("Jason Neis", "Jason"));

// function getFileName(file) {
//   const dotIndex = file.lastIndexOf(".");
//   if (dotIndex === -1) {
//     return file;
//   } else {
//     return file.slice(0, dotIndex);
//   }
// }

// console.log(getFileName("index.css"));

// function createFileName(name, ext) {
//   const nameOll = name.trim();
//   return `${nameOll}.${ext}`;
// }

// console.log(createFileName(" order ", "txt"));

// function calculateTotal(number) {
//   let total = 0; // Змінна для зберігання суми
//   let i = 1; // Лічильник, який починається з 1

//   while (i <= number) {
//     // Виконуємо цикл, доки i не перевищує number
//     total += i; // Додаємо поточне значення i до total
//     i++; // Збільшуємо лічильник
//   }

//   return total; // Повертаємо загальну суму
// }

// console.log(calculateTotal(18));

// function calculateTotal(number) {
//   let total = 0;
//   for (let index = 1; index <= number; index++) {
//     total += index;
//   }
//   return total;
// }

// console.log(calculateTotal(18));
